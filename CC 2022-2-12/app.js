//Create and array of numbers. Sum all of the numbers. Alert the sum.
function sum(arr){
    for(i=0;i<arr.length;i++){
        return arr.map(arr[i]+=)
    }
}
sum([1,2,3,4,5,6])

//Teacher soln
let nums = [10,20,30,50]
alert(nums.reduce((acc,c)=>acc+c, 0))
//I totally forgot about reduce. Dur!