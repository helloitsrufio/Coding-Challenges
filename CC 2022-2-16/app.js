// Declare a variable and assign it to a sentence as a string. Alert if the sentence is a question.
let str = 'RUFIO RUFIO RUFIOOOOOOO'

function checkQuestion(str){
    str.split('')
    if((RegExp/[?]/g)){
        alert("It's a question yoooo")
    }
}
//Yeah so...this does not work. It doesn't I thought maybe the RegEx would work but it didn't.